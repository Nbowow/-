import styled from "styled-components";
import { flexAlignStartStyle } from "../../../styles/common";

export const TableWrapper = styled.div`
    ${flexAlignStartStyle}
    width: 50%;
    height: auto;
`;
